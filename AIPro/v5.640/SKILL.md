---
name: gcc-context
description: Local Claude skill mirror for gcc-evo v5.405. Use when the user wants to inspect, update, validate, or run gcc-evo commands, dashboard, handoff, pipeline, memory, or release packaging from the local Windows workspace.
---

# gcc-context

Use this skill when the task is about `gcc-evo`, `.GCC`, handoff flow, pipeline tasks, dashboard export, release packaging, or syncing the local Claude skill mirror.

## Canonical structure rule

- Canonical source for current program structure: `.GCC/ENTRYPOINTS.md`
- Do not treat this file as the whole-repo source of truth for runtime architecture.
- Use this file only for GCC and skill workflow guidance.
- Treat `.GCC/skill/INDEX.md` as a knowledge-card index, not the architectural source of truth.
- Treat `.claude/skills/codemap-llm-server-v3570/` as a historical mirror for `v3570` only.

## Primary entrypoints

- Preferred command: `gcc-evo <command>`
- Direct script fallback: `python C:/Users/baode/.claude/skills/gcc-context/gcc_evo.py <command>`
- Package fallback: `python -m gcc_evolution <command>`

## Current local baseline

- Skill mirror version: `5.405`
- Workspace root: current repository root
- Project GCC source of truth: `.GCC/`
- Opensource release source: `opensource/`
- KEY-010 module reference: `signal_direction_filter.py`
- key009 signal_filter field: `review_status.signal_filter`
- 当前 L2 score range: 总分 `±16`

## Working rules

- Read `.GCC/ENTRYPOINTS.md` when the task needs current project architecture or runtime truth.
- Read `.GCC/pipeline/tasks.json` for task truth, not stale dashboard exports.
- If dashboard content is missing, regenerate `.GCC/dashboard.html` instead of editing the HTML manually.
- For opensource release sync, align version strings first, then rebuild the zip, then verify command smoke checks.
- Keep `.claude/skills/gcc-context` self-contained enough that `python .../gcc_evo.py version` works even if global PATH is stale.
- For KEY-010 work, treat `.GCC/signal_filter/valid_signals.jsonl`, `.GCC/signal_filter/filtered_signals.jsonl`, and `.GCC/signal_filter/direction_log.jsonl` as the live data files.

## Post-handoff requirement

- After `gcc-evo ho create`, `gcc-evo` must auto-update the mirrored skill documentation.
- The mirrored skill must remain aligned with `.GCC/ENTRYPOINTS.md`, which is the only structure source of truth.
- If current program entrypoints or runtime structure changed in this session, the skill mirror must be refreshed before the handoff flow is considered complete.
- Mirror sync is allowed to be best-effort, but any failure must be surfaced explicitly in command output.

## Local utility scripts

- `ocr.py`
- `load_db.py`
- `ocr_pdf.sh`

Use them only when the user is explicitly working on OCR or paper ingestion.

<!-- gcc-evo:runtime-structure:start -->
## Runtime Structure Mirror

- Auto-generated by `gcc-evo ho create` doc sync.
- Structure source of truth: `.GCC/ENTRYPOINTS.md`
- Do not edit this block manually.

| Entrypoint | Purpose | Start |
|------|------|----------|
| `llm_server_v3640.py` | Main trading service for L1/L2, plugins, gating, `/key009`, `/key010`, and `/key009/gcctm` | `python llm_server_v3640.py` |
| `price_scan_engine_v21.py` | Price scan engine | `python price_scan_engine_v21.py` |
| `monitor_v3640.py` | Realtime monitoring dashboard | `python monitor_v3640.py` |
| `vision_analyzer.py` | Vision analysis service | `python vision_analyzer.py` |
| `filter_chain_worker.py` | FilterChain worker for local to OneDrive sync | `python filter_chain_worker.py --loop` |
| `tv_l2_10m_server.py` | TradingView L2 10 minute service | `python tv_l2_10m_server.py` |
| `.GCC/gcc_evo.py` | GCC-EVO script entry for handoff, dashboard, pipeline, commit, and health | `python .GCC/gcc_evo.py <command>` |
| `key009_audit.py` | KEY-009 audit, backfill, and live dashboard export | `python key009_audit.py` |
| `log_analyzer_v3.py` | Log analyzer with 20 detection groups | `python log_analyzer_v3.py` |
| `.GCC/signal_filter/` | KEY-010 signal filter state and audit logs | jsonl state files |

<!-- gcc-evo:runtime-structure:end -->
