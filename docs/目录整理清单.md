# 当前目录完整整理清单（基于最新框架）

> 目标：不改交易逻辑，仅整理目录结构与文档口径，降低维护成本与误改风险。
>
> 当前主框架基线：`llm_server_v3640.py` + `price_scan_engine_v21.py` + `vision_analyzer.py` + `filter_chain_worker.py` + `monitor_v3640.py` + `log_analyzer_v3.py`

---

## Phase 0 - 整理前冻结与快照

- [ ] 执行一次工作区快照（`git status` + 关键目录树导出）
- [ ] 标记本次整理为“结构整理，不改策略逻辑”
- [ ] 确认运行入口不变：
  - [ ] `llm_server_v3640.py`
  - [ ] `price_scan_engine_v21.py`
  - [ ] `vision_analyzer.py`
  - [ ] `monitor_v3640.py`
  - [ ] `filter_chain_worker.py`
  - [ ] `log_analyzer_v3.py`
- [ ] 记录回滚点（分支名/提交点）

---

## Phase 1 - 根目录分层（先分类，不移动）

### 1) 核心运行层（保留根目录）

- [ ] 核对并保留：
  - [ ] `llm_server_v3640.py`
  - [ ] `price_scan_engine_v21.py`
  - [ ] `vision_analyzer.py`
  - [ ] `brooks_vision.py`
  - [ ] `monitor_v3640.py`
  - [ ] `log_analyzer_v3.py`
  - [ ] `timeframe_params.py`

### 2) 业务插件层（建议集中到 `plugins/`）

- [ ] 规划迁移（先建映射表）：
  - [ ] `supertrend_plugin_v08.py`
  - [ ] `supertrend_av2_plugin.py`
  - [ ] `rob_hoffman_plugin.py`
  - [ ] `chan_bs_plugin.py`
  - [ ] `double_pattern_plugin.py`
  - [ ] `feiyun_plugin.py`
  - [ ] `macd_divergence_plugin.py`
  - [ ] `chandelier_zlsma_plugin.py`
  - [ ] `n_structure_plugin.py`

### 3) 运维脚本层（建议集中到 `tools/ops/`）

- [ ] 规划迁移：
  - [ ] `freeze_stock_sell.py`
  - [ ] `reset_quota_patch.py`
  - [ ] `set_bias.py`
  - [ ] `coinbase_sync_v6.py`
  - [ ] `cycle_switch_trigger.py`

### 4) 分析与回测工具层（建议 `tools/analysis/` + `tools/backtest/`）

- [ ] 规划迁移：
  - [ ] `trade_retrospective.py`
  - [ ] `filter_chain_retrospective.py`
  - [ ] `p0_plugin_analysis.py`
  - [ ] `factor_analyze.py`
  - [ ] `factor_backfill.py`
  - [ ] `backfill.py`
  - [ ] `vision_report.py`

### 5) 训练与数据工程层（建议 `tools/train/`）

- [ ] 规划迁移：
  - [ ] `auto_label_and_train_cnn.py`
  - [ ] `auto_label_and_train_image_cnn.py`
  - [ ] `train_xgboost_v3320.py`

---

## Phase 2 - 运行状态文件收口（高优先级）

### 1) 根目录 JSON 状态文件迁移到 `state/`

- [ ] 建立目标分层：
  - [ ] `state/l1/`
  - [ ] `state/l2/`
  - [ ] `state/plugins/`
  - [ ] `state/vision/`
  - [ ] `state/runtime/`

- [ ] 待迁移样例（按模块落盘）：
  - [ ] `global_trend_state.json`
  - [ ] `l1_plugin_state.json`
  - [ ] `l2_gate_state.json`
  - [ ] `l2_10m_state.json`
  - [ ] `scan_state.json`
  - [ ] `scan_signals.json`
  - [ ] `scan_heartbeat.json`
  - [ ] `plugin_profit_state.json`
  - [ ] `scan_*_state.json`（全部）

- [ ] 迁移前检查引用点（硬编码路径）
- [ ] 迁移后为旧路径提供兼容读取（过渡期）

### 2) 日志与报告收口

- [ ] 确保运行日志只写入 `logs/`
- [ ] 临时报表/审计结果统一到 `logs/reports/` 或 `reports/`
- [ ] 清理根目录遗留报告文件（保留索引）

---

## Phase 3 - 重复目录去重（高风险，分批做）

- [ ] 评估并定唯一源目录：
  - [ ] `modules/` vs `modules2/`
  - [ ] `config/` vs `configs/`
  - [ ] `improvement/` vs `.GCC/improvement/`

- [ ] 处理策略：
  - [ ] 选主目录（source of truth）
  - [ ] 旧目录改为 `archive/legacy_*`
  - [ ] README/Skill 文档写清“唯一入口”

---

## Phase 4 - 文档口径统一

- [ ] 同步三份核心文档版本表：
  - [ ] `README.md`
  - [ ] `.GCC/skill/SKILL.md`
  - [ ] `C:\Users\baode\.claude\skills\Aipro\skill.md`

- [ ] 统一关键口径：
  - [ ] FilterChain 是否为唯一过滤入口
  - [ ] 移动止损/止盈是否豁免过滤链
  - [ ] KNN 描述统一为 `modules/knn` 五层架构

- [ ] 在 `.GCC/skill/evolution-log.md` 记录每次目录整理变更

---

## Phase 5 - 迁移执行顺序（建议）

- [ ] 批次A：仅移动工具脚本（不影响运行主链）
- [ ] 批次B：状态文件路径收口 + 兼容层
- [ ] 批次C：重复目录去重（单独回归测试）
- [ ] 每批次后执行：
  - [ ] 启动主服务
  - [ ] 启动扫描引擎
  - [ ] 启动监控
  - [ ] 日志分析命令验证

---

## 验收清单（完成标准）

- [ ] 根目录仅保留“入口脚本 + 关键文档 + 必要配置”
- [ ] 运行时状态文件全部从 `state/` 读取/写入
- [ ] 不再存在双份主目录引发歧义（modules/config/improvement）
- [ ] 三份文档版本口径一致
- [ ] 无新增交易逻辑变更（仅结构/路径变更）
- [ ] 能通过一轮完整日常运行流程

---

## 可选增强（后续）

- [ ] 增加 `scripts/check_structure.py`，自动检查目录规范
- [ ] 增加 pre-commit 检查：禁止新的根目录状态文件
- [ ] 增加 `docs/PROJECT_STRUCTURE.md` 维护目标目录树
